项目背景：
1. 要求根据已有的法规、材料、规则对单位发布的招标文件进行智能审批，检查内容是否符合要求；
2. 由于对审批的时间有要求，所以目前期望以代码的形式进行校验，即利用一些开源的Code-Agent框架根据规则生成对应的校验代码（如Python），利用代码进行检索，并生成对应的报告；

技术路线：
1. 目前技术路线大致分为3步，分别是目录/关键词提取、关键词匹配、代码校验；
2. 目录/关键词提取：由于招标文件通常具有比较统一的写作规范，所以可以先通过LLM将招标文件按照目录拆分，并总结每个子部分的关键词和相关内容，方便后续进行匹配；
3. 关键词匹配：这一步是将上一步提取出的关键词和评审规则进行对应，目的是提取出文章中对应每条审批规则的内容，减少代码的；
4. 代码校验：这一步是利用开源的Code-Agent框架先生成代码，然后根据相关的内容做校验，并输出校验结果；

项目约束：
1. 当前不用管目录/关键词提取和关键词匹配，本项目**专注于代码校验部分**；
2. 由于项目甲方要求，Code-Agent最好不用调用API的形式，通过本地部署大模型的方式提供服务；
3. 经过前期的调研，可能比较符合我们场景的开源框架有：OpenHands、LangGraph，比较多star；

核心需求：
1. 根据以上内容，作出规划，回答以下问题：我这个需求用什么框架比较好？搭配什么本地模型比较好？如果对本地算力有需求，每个模型需要多少预估一下；
2. @rules/ 里面是我摘出来的一些实际会用到的规则样例，项目部署时实际的规则是这个的超集，先根据这些内容做试点；
3. 如果大致确定好所用的框架，可以跟我沟通要怎么部署、怎么接本地模型、怎么一步步完成需求；

  ┌────────────────────────────────────────┬──────────────┬────────────────┐
  │               你想测什么               │ 要不要起模型 │      入口      │
  ├────────────────────────────────────────┼──────────────┼────────────────┤
  │ 运行时：给定“已抽取内容”，跑校验出报告 │ 不要         │ main.py        │
  ├────────────────────────────────────────┼──────────────┼────────────────┤
  │ 编写期：规则 md → 本地模型生成 checker │ 要 vLLM      │ gen_checker.py │
  └────────────────────────────────────────┴──────────────┴────────────────┘

  下面按「只测运行时」为主说明（最常见）。

  ---
  1. 入口在哪

  仓库根目录
  └── main.py          ← 步骤三运行时唯一入口

  读入一份 JSON（模拟步骤一/二已抽好的内容）→ 跑 src/checkers → 写报告到 reports/。
  不读 PDF，不抽关键词。

  ---
  2. 命令怎么启动

  source /home/zyl/miniconda3/etc/profile.d/conda.sh
  conda activate approval
  cd /home/zyl/private/Coding/IntelligentApproval

  # 用自带样例跑一遍
  python main.py --input data/inputs/sample_tender.json --out reports/

  # 只跑部分规则
  python main.py --input data/inputs/sample_tender.json --rules rule_2 rule_4 --out reports/

  # 可选：确定性单测（不依赖你的样例文件）
  python tests/test_checkers.py

  输出：

  - reports/<输入文件名>.json
  - reports/<输入文件名>.md

  终端会打印：总体结论: violation|warning|pass | 违规 n 预警 m。

  ---
  3. 测试样例放哪、什么格式

  放哪

  data/inputs/          ← 你构造的“上游已抽取内容”都放这里
    sample_tender.json  ← 现成样例，可复制改名
    my_case_01.json     ← 你自己的

  路径任意，只要 main.py --input 指过去即可；约定放 data/inputs/ 方便管理。

  格式（TenderContext JSON）

  根对象字段如下（都可省略；某规则缺它要的字段会返回 NA，不会崩）：

  {
    "doc_name": "可选，报告标题用",
    "full_text": "可选，全文或拼接文本",
    "sections": {
      "项目概况": "该章节正文……"
    },
    "fields": {
      "project_type": "信息系统/软件开发",
      "contract_amount": 20000000,
      "warranty_months": 12,
      "response_hours": 4,
      "registered_capital_req": 50000000,
      "project_actual_capital_need": 20000000,
      "building_height": null,
      "required_qualification_level": null
    },
    "tables": {},
    "references": {}
  }

  对照现成样例：data/inputs/sample_tender.json。

  各规则大概吃哪些字段（构造时按这个填）

  ┌──────────────────┬───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┬────────────────────────────────────────────┐
  │       规则       │                                                            主要读什么                                                             │             你怎么模拟“已抽取”             │
  ├──────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────┤
  │ rule_2 模糊词    │ sections["项目概况"]，没有则用 full_text                                                                                          │ 把“项目概况”相关段落粘进 sections.项目概况 │
  ├──────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────┤
  │ rule_4 质保/响应 │ fields.project_type / warranty_months / response_hours                                                                            │ 填数值，单位：月 / 小时                    │
  ├──────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────┤
  │ rule_6 资本/资质 │ fields.registered_capital_req、project_actual_capital_need 或 contract_amount；可选 building_height、required_qualification_level │ 填金额与资质相关数字/等级                  │
  ├──────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────┤
  │ rule_5（可略过） │ full_text                                                                                                                         │ 全文里是否有地方保护表述                   │
  ├──────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────┤
  │ rule_1（可略过） │ tables                                                                                                                            │ 暂未作为重点                               │
  └──────────────────┴───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┴────────────────────────────────────────────┘

  原则：

  1. 打开对应 rules/rule_N.md 的 【开发说明】→ 输入数据。
  2. 把“上游应抽出的那一段/那几个字段”写进 JSON 的 sections 或 fields。
  3. 只测某条规则时，其它字段可以不填，或用 --rules rule_2 只跑那条。

  最小可跑例子（只测 rule_2）

  data/inputs/only_rule2.json：

  {
    "doc_name": "只测模糊词",
    "sections": {
      "项目概况": "本项目工期视情况而定，费用酌情处理。"
    }
  }

  python main.py --input data/inputs/only_rule2.json --rules rule_2 --out reports/

  最小可跑例子（只测 rule_4）

  {
    "doc_name": "只测质保",
    "fields": {
      "project_type": "信息系统/软件开发",
      "warranty_months": 6
    }
  }

  python main.py --input data/inputs/only_rule4.json --rules rule_4 --out reports/

  ---
  4. 和「编写期」的区别（避免走错入口）

  ┌────────────────────────┬────────────────────┬──────────────────────────────────┐
  │                        │    运行时步骤三    │      编写期（生成 checker）      │
  ├────────────────────────┼────────────────────┼──────────────────────────────────┤
  │ 入口                   │ main.py            │ gen_checker.py                   │
  ├────────────────────────┼────────────────────┼──────────────────────────────────┤
  │ 输入                   │ data/inputs/*.json │ rules/*.md + acceptance 测试     │
  ├────────────────────────┼────────────────────┼──────────────────────────────────┤
  │ 是否起 vLLM            │ 否                 │ 是：bash scripts/serve_llm_3b.sh │
  ├────────────────────────┼────────────────────┼──────────────────────────────────┤
  │ 你现在若只验证校验逻辑 │ ✅ 用这个          │ 先不用                           │
  └────────────────────────┴────────────────────┴──────────────────────────────────┘

  ---
  一句话：
  只测步骤三运行时 → 在 data/inputs/ 按上面 JSON 契约造样例 →

  python main.py --input data/inputs/你的文件.json --out reports/